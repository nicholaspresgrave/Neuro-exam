version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    build:
      html:
        - mkdir -p $READTHEDOCS_OUTPUT/html
        - cp index.html app.js styles.css manifest.webmanifest service-worker.js icon.svg $READTHEDOCS_OUTPUT/html/
